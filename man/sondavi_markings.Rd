% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{sondavi_markings}
\alias{sondavi_markings}
\title{One row per marked cell or pin, for a heatmap}
\usage{
sondavi_markings(data, columns = NULL)
}
\arguments{
\item{data}{The result of \code{\link[=sondavi_responses]{sondavi_responses()}} or
\code{\link[=sondavi_snapshot_responses]{sondavi_snapshot_responses()}}, before \code{\link[=sondavi_unnest]{sondavi_unnest()}}.}

\item{columns}{Which questions. Default: every image marking question found.}
}
\value{
A data frame with \code{response_id}, \code{respondent_id} (when the data has
it), \code{completed_at}, \code{question}, \code{category}, \code{row}, \code{col}, \code{x_norm},
\code{y_norm}, \code{x_px}, \code{y_px} and \code{image_src}. People who marked nothing
contribute no rows.
}
\description{
Image marking questions arrive as one list column each, holding the stored
answer: the image it was given on, the grid, and the painted cells or the pins.
This turns them into the long table a heatmap is drawn from - the same rows and
columns as \code{markings.csv} in the platform's image-markings export, so a script
can switch between the file and the API without changes.
}
\details{
Every row carries its position twice: \code{x_norm}/\code{y_norm} between 0 and 1 (left
to right, top to bottom), and \code{x_px}/\code{y_px} in pixels of the original image -
the mapping, already done. For a painted cell that is the cell's centre; \code{row}
and \code{col} say which cell (counted from 0 at the top left) and are \code{NA} for pins.
A cell covers \code{x_norm} from \code{col / cols} to \code{(col + 1) / cols}.

Nothing is aggregated: no densities, areas or centroids.
}
\examples{
\dontrun{
d <- sondavi_responses(con, 42)
m <- sondavi_markings(d)
green <- subset(m, question == "map" & category == "green")
table(green$row, green$col)          # how many people marked each cell
}
}
